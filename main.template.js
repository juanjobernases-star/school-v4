const {app,BrowserWindow,ipcMain}=require('electron');const path=require('path');const http=require('http');function create(){const w=new BrowserWindow({width:1280,height:850,webPreferences:{preload:path.join(__dirname,'preload.js'),sandbox:true,contextIsolation:true,nodeIntegration:false,webSecurity:true}});w.webContents.session.webRequest.onHeadersReceived((d,cb)=>cb({responseHeaders:{...d.responseHeaders,'Content-Security-Policy':["default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data:; connect-src 'self' http://localhost:11434 https://school-gemini-proxy.pechicolo.workers.dev; object-src 'none'; base-uri 'self'; frame-ancestors 'none'; form-action 'self';"]}}));w.loadFile('index.html')}ipcMain.handle('ollama-chat',async(e,prompt)=>new Promise(resolve=>{const b=JSON.stringify({model:'gemma2:2b',prompt:String(prompt).slice(0,1000),stream:false});const r=http.request({hostname:'localhost',port:11434,path:'/api/generate',method:'POST',headers:{'Content-Type':'application/json','Content-Length':Buffer.byteLength(b)},timeout:60000},res=>{let data='';res.on('data',c=>data+=c);res.on('end',()=>{try{resolve({success:true,response:JSON.parse(data).response||'Sin respuesta'})}catch{resolve({success:false,error:'Respuesta no válida'})}})});r.on('error',err=>resolve({success:false,error:err.message}));r.on('timeout',()=>{r.destroy();resolve({success:false,error:'Timeout'})});r.write(b);r.end()}));app.whenReady().then(create);app.on('window-all-closed',()=>{if(process.platform!=='darwin')app.quit()});